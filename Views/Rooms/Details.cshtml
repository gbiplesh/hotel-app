@model HotelApp.Models.Rooms

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Rooms</h4>
    <hr />

    <div>

        @*<form target="_self">*@

     <form asp-action="RoomsSelected" asp-route-id="@Model?.Id" method="post">
         
        
        <dl class="row">
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.RoomType)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.RoomType)
            </dd>
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.Price)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.Price)
            </dd>
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.Available)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.Available)
            </dd>
        </dl>

            <div class="col-md-4">

                    <div>
                        <label for="FullName" class="control-label">Full Name</label>
                        <input name="FullName" class="form-control" />
                    </div>
                    <div class="form-group">
                        <label for="CheckIn" class="control-label">Check-In Date</label>
                        <input name="CheckIn" type="datetime-local" class="form-control" />
                    </div>
                    <div class="form-group">
                        <label for="CheckOut" class="control-label">Check-Out Date</label>
                        <input name="CheckOut" type="datetime-local" class="form-control" />
                    
                    </div>
                    <div>
                        <label for="RoomQuantity" class="control-label">No. of Rooms</label>
                        <input name="RoomQuantity" class="form-control" />
                    </div>
                    <br />
                    <div class="form-group">
                        <input type="submit" value="Book" class="btn btn-primary" />
                    </div>
                
            </div>

    </form>    
     </div>

</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
